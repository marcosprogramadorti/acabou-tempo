import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Acabou Tempo',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
